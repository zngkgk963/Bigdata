git remote add origin https://github.com/zngkgk963/bigdata.git
git push -u origin master

https://github.com/zngkgk963/bigdata.git

*빅데이터 폴더를 생성하여 파일을 저장합니다.

폴더 및 파일삭제

rm -rf bigdata

파일만 clone 하는 방법

git clone https://github.com/zngkgk963/bigdata.git .


Git, Github 사용하기

Git이란? 
버전 관리 시스템의 한 종류입니다.

버전 관리
여러 파일을 하나의 버전으로 묶어 관리하는 것을 말합니다.

버전 관리 시스템의 종류
1. 클라이언트-서버 모델
하나의 중앙 서버로 여러 클라이언트들이 각자 필요한 데이터만 가져와서 작업을 하고, 
다시 중앙 서버로 보내어 통합하는 방식을 말합니다. (CVS, Subversion)

2. 분산 모델
하나의 중앙 서버가 존재하지만 여러 클라이언트들은 각자의 컴퓨터 저장소에 전체 사본을 가지고 작업하는
방식을 말합니다. (Git)

Git의 장점
- 동시에 작업하는 사람들과 소스코드를 주고 받을 필요가 없습니다.
- 같은 파일을 여려명이 동시에 병렬 개발이 가능합니다.
- 변동 과정을 체계적으로 관리할 수 있고, 언제든지 지난 시점의 버전으로 되돌릴 수 있습니다.
- 인터넷이 연결되지 않은 곳에서도 개발을 진행할 수 있으며, 중앙 서버의 데이터가 유실되어도
다시 복구할 수 있습니다.


Git 설치
1. cmd에서 git을 입력하고 설치되어 있는지 확인합니다.
'git'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.

2. 구글에서 '깃 설치'로 검색 후 사이트로 이동합니다.

3. 왼쪽 Download를 클릭하고 운영체제에 맞는 프로그램을 다운로드합니다.

4. 다운로드 받은 프로그램을 실행하고 기본설정을 유지하고 설치를 완료합니다.

5. Git Bash를 실행하고 git을 입력하면 git 명령어가 출력됩니다.

Github란?
git으로 버전 관리한 코드를 올릴 수 있는 클라우드 서버입니다. 단순히 저장만 하는 것이 아니라
다른 유저들과 함께 코드를 공유하고 온라인으로 하나의 프로그램을 같이 제작할 수 있게 합니다.

Github 가입
https://github.com 에서 Sing up for Githup를 클릭하고 가입을 진행합니다.

유저 이름 : Minuuuuu
아이디 : schoolnick@naver.com
비밀번호 : dlalsdn5908

로컬 저장소 생성하기
1. 내 컴퓨터에 Git을 사용할 폴더를 만듭니다.
2. Git Bash로 원하는 폴더에 접근합니다. ( 폴더 안에서 shift + 마우스 오른쪽 버튼 > git bash here 선택)
pwd : 현재 디렉토리 경로를 표시합니다.
ls : 현재 디렉토리(폴더)의 디렉토리와 파일을 표시합니다.
cs : 다른 디렉토리로 이동합니다.
(cs .. : 상위 디렉토리로 이동, cs 디렉토리 이름 : 해당 디렉토리로 이동)

3. git init 명령어로 로컬 저장소를 만듭니다.
정상적으로 로컬 저장소가 만들어진다면 아래와 같이 출력됩니다.
Initialized empty Git repository in C:/Users/Administrator/Desktop/프로그래밍/GitProject/.git/

* 폴더가 안보일 떄
ls -al

# : 주석처리

4. 버전관리 파일 선택하기
git add README.md

5. 버전관리 파일 커밋하기
git commit -m "README.md 파일 버전을 만들어요"

아래와 같이 설정합니다.
git config --global user.email "schoolnick@naver.com"
git config --global user.name "이민우"

git commit -m "README.md 파일 버전을 만들어요"

* git config 삭제하기
git config --unset user.name
git config --unset user.email

* git config 확인하기
git config --list

로그 확인하기
git log

여러 파일을 선택하기
git add .

여러 파일 커밋하기
git commit -m "index.html, style.css 파일 추가"

로컬 저장소에 github 저장소의 주소를 알려주기
git remote add origin https://github.com/Minuuuuu/bigdata.git

만든 커밋을 github에 푸시하기
git push -u origin master > 로그인 창 뜨면 수락 > 비밀번호 입력 


원격 저장소(github)의 내 컴퓨터에 받아오기
git clone https://github.com/Minuuuuu/bigdata.git
* bigdata 폴더를 생성하여 파일을 저장 

폴더 및 파일 삭제
rm -rf bigdata

파일만 clone 하는 방법
git clone https://github.com/Minuuuuu/bigdata.git .


실습
README.md, index.html 파일을 아래와 같이 수정하고 github에 추가 푸시하기
README.md
# 2번째로 작성하는 README.md
# 추가로 커밋합니다.

index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>첫 페이지</title>
    <link rel="stylesheet" href="./style.css" type="text/css">
</head>
<body>
    <h2>첫 페이지</h2>
    <p>류정원의 홈페이지에 오신걸 환영합니다.</p>
    <p>copyright 2021 류정원</p>
</body>
</html>


github 다른사람과의 협업하기
repository 선택 -> setting -> manage access

sourcetreeapp.com